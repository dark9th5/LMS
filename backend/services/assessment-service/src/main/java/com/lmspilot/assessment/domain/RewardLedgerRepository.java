package com.lmspilot.assessment.domain; import java.util.*; import org.springframework.data.jpa.repository.*; import org.springframework.data.repository.query.Param; import jakarta.persistence.LockModeType; public interface RewardLedgerRepository extends JpaRepository<RewardLedgerEntity,UUID> { boolean existsByCompetitionIdAndUserIdAndRewardId(UUID c,UUID u,UUID r); List<RewardLedgerEntity> findAllByCompetitionIdOrderByCreatedAtAsc(UUID id); }
